/dist
/node_modules
/.expo
/android
/ios
/.claude
package-lock.json

# Docs are maintained by hand (bilingual, table-aligned) — don't reflow.
**/*.md

# Interactive guide mockups are hand-authored and frozen as a record of an
# earlier screen layout, so reflowing them only costs review noise.
docs/guides/**/*.html

# Downloaded from the Firebase console verbatim. Formatting it here would be
# undone by the next download, so leave it exactly as the console produces it.
google-services.json
