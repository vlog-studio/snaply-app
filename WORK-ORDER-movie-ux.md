# 무비 탭 · 무비 상세화면 UX 개선 작업 지시서

> **임시 문서입니다.** 최초 작업 지시로만 쓰고, 항목이 모두 소화되면 삭제합니다.
> `AGENTS.md`의 문서 인덱스에 등록하지 않으며, 다른 작업이 참고할 성격의 문서가 아닙니다.
> 각 항목을 실제로 구현할 때의 사실 원천은 언제나 `docs/features/movie.md`와
> `docs/ux/`입니다.

작성일: 2026-08-13
대상: `src/pages/movies` (무비 탭), `src/pages/movie` (무비 상세), `src/widgets/movie-shelf`
근거: `docs/ux/principles.md`, `docs/ux/ux-smells.md`, `docs/features/movie.md`,
그리고 백엔드 저장소(`../snaply-backend`, `main` 기준)의 `docs/api-spec.md`,
`docs/decisions/plan-limits.md`, `docs/decisions/credit-payment-model.md`

---

## 0. 백엔드 확인 결과 요약

이 문서의 A/B 분류는 아래 사실 위에서 나눴습니다.

| 확인한 것 | 결과 | 출처 |
| --- | --- | --- |
| `POST /edit-jobs`가 받는 필드 | `clips` · `stylePreset` · `outputProfile` · `fitMode` · `subtitles` | api-spec.md §AI 편집 |
| BGM 트랙 지정 | **없음.** 파이프라인이 `stylePreset`으로 트랙을 고른다 | api-spec.md (요청 바디에 음악 필드 없음) |
| 편집 작업 취소 | **없음.** 라우트는 `POST /edit-jobs` · `GET /edit-jobs/:id` · WS 진행률뿐 | `apps/api/src/routes/edit-jobs.ts` |
| 월 3편 제한 | **집행하지 않음.** 2026-08-05 제거, 복원 계획 없음 | decisions/plan-limits.md |
| 과금 모델 | 구독 제거 → 크레딧 차감으로 전환 **결정**, 수량·가격·차감량은 **미확정** | decisions/credit-payment-model.md, backlog.md A-2 |
| 자막 | `subtitles` 플래그가 스펙에 있고 기본 false. 앱은 보내지 않는다 | api-spec.md |

---

## A. 백엔드와 무관하게 지금 처리할 것

### A-1. 무비 탭 빈 상태에 전진 액션 추가 (높음) — ✅ 2026-08-13 처리 완료

> 빈 상태에 `스냅 골라 새 무비` 버튼을 넣고 `/snaps?select=1`로 보낸다 — 스튜디오의
> 새 무비 행과 같은 행선지, 접근성 라벨도 같은 문장(`스냅 골라서 새 무비 만들기`).
> 설명 문구는 늘리지 않았다. 실기기에서 빈 상태 렌더와 스냅 피커 도착까지 확인.


- **현상**: [`movies-page.tsx:209`](src/pages/movies/ui/movies-page.tsx:209) — 점선 박스에
  `아직 만든 무비가 없어요` 한 줄뿐. 무비를 만들려면 사용자가 스튜디오 탭을 스스로 찾아야 한다.
- **스멜/원칙**: `Dead End` · 원칙 15(Preserve User Control and Exit), 3(Action First)
- **끝난 상태**: 빈 상태에 스냅을 골라 새 무비로 가는 액션이 하나 있고, 그것이 그 영역에서
  가장 뚜렷한 컨트롤이다. 설명 문장은 늘리지 않는다(액션은 copy가 아니므로
  `Show State, Not Instructions`와 충돌하지 않는다).

### A-2. watch mode의 facts line이 완성본의 사실을 말하게 하기 (높음) — ✅ 2026-08-13 처리 완료

> `MovieRender.style`을 추가하고 `finishMovieJob`이 컷 목록과 함께 얼린다. watch facts는
> `render.style`을 읽고, 그 필드가 없는 옛 렌더는 스타일 항목 자체를 생략한다(추측 금지).
> 실기기에서 실제 백엔드로 검증: 감성으로 생성 → facts `감성`, 스튜디오에서 여행으로
> 바꿔도 facts는 `감성` 유지(`movie.style=travel`, `render.style=emotional`).


- **현상**: [`movie-watch.tsx:78`](src/pages/movie/ui/movie-watch.tsx:78)이 `movie.style` /
  `movie.bgm`, 즉 **지금 저장된 값**을 읽는다. 스튜디오에서 스타일만 바꾸고 나가면 완성본은
  `일상`으로 만들어졌는데 화면은 `감성`이라고 쓴다. 컷 구성 drift는 `render.snapRefs`로
  감지해 알림까지 띄우면서 스타일 drift는 감지하지 않는다.
- **스멜/원칙**: 상태 read-out이 거짓 · 원칙 13(Show State, Not Instructions)
- **끝난 상태**: facts가 `render` 스냅샷에서 읽힌다. 렌더에 스타일이 저장돼 있지 않으면
  `entities/movie`의 render 타입에 추가하고 `finishMovieJob`이 함께 얼린다.
  BGM 항목은 A-3에 따라 이 줄에서 뺀다.
- **주의**: 스냅샷 없는 옛 렌더는 `watch-cuts.ts`가 라이브 목록으로 폴백하는 것과 같은 규칙을
  따른다 — 추측해서 채우지 않는다.

### A-3. 세부 시트의 배경 음악 컨트롤 처리 (높음) — ✅ 2026-08-13 제거로 처리 완료

> 결정: 제거(B-1은 따라서 닫힘 — 백엔드에 트랙 필드를 요구하지 않는다).
> 세부 시트의 pill, 세부 칩의 둘째 줄, watch facts의 BGM이 모두 사라졌고
> `Movie.bgm`은 저장만 유지한다. 세부 칩의 둘째 줄은 순서 고정 read-out이 대신한다.
> A-2에서 BGM으로 인한 부정확은 이걸로 닫혔고, **스타일 drift는 그대로 남아 있다.**


- **현상**: [`detail-sheet.tsx:63`](src/pages/movie/ui/detail-sheet.tsx:63)이 5개 트랙을
  라디오로 제공하지만 `POST /edit-jobs`에 음악 필드가 없어 선택이 렌더에 도달하지 않는다.
  사용자가 고른 트랙과 실제 완성본의 음악이 무관하다.
- **스멜/원칙**: `Silent Automation`, `Phantom Affordance` · 원칙 15, 5
- **선례**: 2026-08-07에 `자동 자막` 스위치를 **정확히 같은 이유로** 제거했다
  (`docs/features/movie.md` → Subtitles 행). 같은 판단을 적용한다.
- **끝난 상태**: 트랙 pill이 사라지고 `Movie.bgm`은 저장만 유지(스위치 제거 때와 동일),
  BGM을 말하던 모든 표면(칩 보조 문구, watch facts)이 함께 정리된다.
- **대안**: 제거 대신 백엔드에 트랙 필드를 요구하는 길은 B-1. **둘 중 하나를 고르는 결정이
  선행**하고, 그건 제품 결정이므로 임의로 정하지 않는다(가드레일 G8).

### A-4. 컷 인스펙터의 ✕ 의미 충돌과 선택 해제 경로 (중간) — ✅ 2026-08-13 처리 완료

> 빼기는 휴지통 아이콘으로, 행 맨 앞에 ✕(컷 선택 해제)를 추가했다.
> 기기 확인함 — ✕ · 컷 1/2 사용 1초 · ◀ ▶ 🗑 한 줄에 들어가고, ✕가 선택을 놓아
> 생성/공유 행이 돌아온다.


- **현상**: [`cut-inspector.tsx:110`](src/pages/movie/ui/cut-inspector.tsx:110)의 ✕는
  "컷 빼기"인데 앱 전반에서 ✕는 닫기다. 게다가 선택을 놓는 명시적 컨트롤이 없어
  스트립 빈 곳 탭을 알아야 하고, 그동안 화면의 주 액션(생성)은 슬롯에서 사라져 있다.
- **스멜/원칙**: `Mystery CTA`, `Hidden Primary` · 원칙 4, 3
- **끝난 상태**: 빼기가 삭제로 읽히는 아이콘(휴지통)을 쓰고, 선택 해제가 인스펙터 안에서
  한 번의 탭으로 가능하다. 슬롯 높이 고정 규칙(스테이지가 남은 높이로 크기를 잡는다)은
  깨지 않는다.

### A-5. 이름 바꾸기를 시트 위의 시트에서 꺼내기 (중간) — ✅ 2026-08-13 처리 완료

> ⋯ 시트 안의 단계로 이동(삭제 확인과 같은 방식). `RenameMovieForm`이 이미 분리돼
> 있어서 그대로 썼다. 스튜디오의 ✎는 시트 그대로(그 화면엔 다른 모달이 없다). 기기 확인함.


- **현상**: [`movie-page.tsx:444`](src/pages/movie/ui/movie-page.tsx:444)이 ⋯ 시트를 닫으며
  rename 시트를 연다. 삭제 확인은 "두 Modal이 애니메이션을 경합한다"는 이유로 같은 시트 안
  단계로 만들어 뒀는데([`movie-actions-sheet.tsx:70`](src/pages/movie/ui/movie-actions-sheet.tsx:70)),
  rename만 그 문제에 그대로 노출돼 있다.
- **스멜/원칙**: `Modal Stack`, `Inconsistent Twin` · 원칙 11, 14
- **끝난 상태**: rename도 같은 시트 안의 두 번째 단계이거나, 최소한 시트 하나가 완전히 닫힌
  뒤 열린다. `features/rename-movie`의 폼은 그대로 재사용한다.

### A-6. 무비 탭 선택 바의 공유를 비활성+사유로 통일 (중간) — ✅ 2026-08-13 처리 완료

> 1편 선택 시 공유는 항상 자리를 지키고, 파일이 없으면 비활성 + 사유 한 줄.
> `busy`는 분리해 `준비 중…`으로 표시. 기기 확인함 — 백엔드가 살아 있을 때는 활성,
> 주소를 못 받는 동안에는 비활성 + 사유(그 사유 자체의 부정확은 A-10).


- **현상**: [`movie-selection-bar.tsx:70`](src/pages/movies/ui/movie-selection-bar.tsx:70)은
  1편 선택 + 렌더 파일 있음일 때만 공유를 그린다. 같은 앱의 다른 두 곳(watch mode 버튼,
  ⋯ 시트 행)은 비활성 + 사유 표기다. 그리드에서는 왜 안 뜨는지 알 방법이 없고,
  `busy`까지 `shareBlocked`에 합쳐져 있어 준비 중에는 버튼이 통째로 사라졌다 돌아온다.
- **스멜/원칙**: `Inconsistent Twin` · 원칙 11
- **끝난 상태**: 공유가 조건과 무관하게 자리를 지키고, 불가할 때 사유가 붙는다.
  `busy`와 `blocked`가 구분돼 준비 중에는 `공유 준비 중…`으로 남는다.

### A-7. 그리드 타일의 실패 알림 축소 (중간) — ✅ 2026-08-13 처리 완료

> 타일에서는 실패 사유를 한 줄로 clamp(`variant="tile"`), 보드 행은 그대로 wrap.
> 기기 확인함 — 실패 상태 무비가 없어 타일 조건을 잠깐 강제해 촬영하고 되돌렸다.
> 사유가 한 줄로 잘리고 알림 블록 높이가 고정돼, 실패한 타일이 있는 행만 예측 가능하게
> 커진다.


- **현상**: [`movie-tile.tsx:126`](src/widgets/movie-shelf/ui/movie-tile.tsx:126)이 타일 아래
  오류 문구 + 재시도를 붙인다. 2열 `flexWrap` 그리드라 같은 행 타일과 높이가 어긋나고,
  좁은 칸에 서버 원문(B-3)이 들어간다.
- **원칙**: 17(Density Where Density Pays) — 그리드는 훑어보는 면이다.
- **끝난 상태**: 타일에는 `실패` 배지와 `다시 시도`만 남고, 이유 전문은 무비 화면에서 읽힌다.
  단, 재시도 자체를 타일에서 없애지는 않는다 — 두 곳에서 같은 복구를 제공한다는 것이
  `movie-shelf`가 이 컴포넌트를 소유하는 이유다(가드레일 G8).

### A-8. 스튜디오 스테이지가 미리보기임을 상태로 표시 (중간) — ✅ 2026-08-13 처리 완료

> 스테이지 우상단 `미리보기` 배지(플레이어의 컷 n/N 반대편). 기기 확인함.


- **현상**: 스튜디오 스테이지는 색보정·음악·자막·배경 처리가 없는 원본 컷을 재생하는데
  화면이 그걸 말하지 않는다. `docs/features/movie.md`의 Known limitations도
  "A disclosure is still owed"로 남겨둔 항목이다.
- **원칙**: 14(Predictable Transitions), 가드레일 G16(구조를 copy로 때우지 않기)
- **끝난 상태**: 설명 문장이 아니라 스테이지 모서리의 한 단어 배지(`미리보기` 수준)로
  상태가 드러난다.

### A-9. 무비 탭 헤더 카운트의 정직성 (낮음) — ✅ 2026-08-13 처리 완료

> `모두 N편`. 기기 확인함.


- **현상**: [`movies-page.tsx:170`](src/pages/movies/ui/movies-page.tsx:170)의 `N편`이
  초안·실패까지 포함한 수라 "완성한 무비 N편"으로 오독된다.
- **끝난 상태**: 카운트 라벨이 무엇을 세는지 오독되지 않는다. **초안과 완성본을 한 그리드에
  두는 결정 자체는 건드리지 않는다** — 확정된 결정이다(가드레일 G22).


### A-10. 완성 파일 주소를 못 받을 때의 상태 — ✅ 2026-08-13 처리 완료

> 요청에 8초 데드라인(`apiRequest`의 opt-in `timeoutMs`)을 주고 이 쿼리의 재시도를 1회로
> 묶어 `resolving`이 끝나게 했다. 실패했는데 저장된 uri도 없으면 `unresolved` —
> 스테이지가 `완성 파일을 불러오지 못했어요` + `다시 시도`를 보여주고 **원본 컷으로
> 넘어가지 않는다**. `ShareBlock`에 `unresolved`를 추가하고 두 문구를
> `ShareBlockMessages`(share-movie)로 모아 세 표면이 같이 쓴다.
> 기기 확인함 — 분기를 잠깐 강제해 스테이지·공유 버튼·⋯ 시트 문구까지 보고 되돌렸다.
> **데드라인 자체가 도는 것은 실기기에서 재현하지 못했다**(백엔드가 살아 있어서
> 도달 불가 상태를 만들 수 없었다). 코드와 유닛 테스트까지만 확인된 부분이다.

- **현상**: `useRenderSource`가 `GET /videos/{id}`를 재시도하는 동안 `resolving`이 계속 true다.
  백엔드에 닿지 못하면 watch mode 스테이지가 `불러오는 중…`에서 멈춘 채로 끝나지 않고,
  선택 바·공유 버튼은 `아직 완성 파일이 만들어지지 않아 공유할 수 없어요`라고 말한다 —
  **파일은 있고, 주소를 못 받은 것뿐이라 사유가 틀렸다.**
- **어떻게 드러났나**: A-6 검증 중 백엔드가 내려간 상태에서 관찰했다. A-6 이전에는 공유가
  아예 사라졌기 때문에 이 잘못된 사유가 화면에 나올 자리가 없었다 — 없앤 게 아니라 드러난
  것이다.
- **원칙**: 16(Errors Are Design Failures First), 13 — `ShareBlock`이 `no-render` 하나뿐이라
  "파일 없음"과 "주소 못 받음"을 구분할 수 없다.
- **할 일**: 재시도에 상한을 두고 실패 상태를 따로 만들기(스테이지의 에러 페이스는 이미 있다),
  `ShareBlock`에 사유 하나 추가. 백엔드 작업은 필요 없다.

---

## B. 백엔드 구현이 선행돼야 하는 것

앱 쪽에서 지금 할 수 있는 일은 "요구사항을 백엔드에 올리는 것"까지다.
아래 항목을 앱에서 임의로 흉내 내지 않는다(가짜 read-out은 A-2가 지적한 결함 그 자체다).

### B-1. BGM 트랙 지정 (A-3의 대안 경로) — ❎ 2026-08-13 채택하지 않음

> A-3을 제거로 결정했으므로 이 요구사항은 백엔드에 올리지 않는다. 아래는 나중에
> 되살릴 때를 위한 기록으로만 남긴다.


- **막힌 지점**: `POST /edit-jobs`에 음악 관련 필드가 없다. 워커가 `stylePreset`으로
  트랙을 고른다.
- **백엔드에 필요한 것**: 요청 바디의 트랙 식별자 필드 + 서버가 제공하는 트랙 카탈로그
  (`GET /bgms`. 앱의 `entities/movie/lib/movie-bgm.ts`는 현재 로컬 상수이자 순수 창작이다).
- **앱 쪽 후속**: 필드가 생기면 세부 시트의 pill이 실제 결정권을 갖는다.
- **결정 필요**: 이 API가 가까운 시일에 나오지 않는다면 A-3(제거)이 답이다. 두 길을 동시에
  열어두면 "아무것도 결정하지 않는 컨트롤"이 계속 남는다.

### B-2. 생성 중 취소 — ✅ 2026-08-13 처리 완료 (백엔드 + 앱)

> 백엔드가 `DELETE /edit-jobs/:id`와 최종 상태 `canceled`를 구현(스펙 갱신 2026-08-13),
> 앱은 `generating` 푸터의 `만들기 취소`(인라인 확인 단계, `CancelRunControl`)로 연결했다.
> 취소된 무비는 `draft`로 복귀 — 규칙은 `docs/features/movie.md` §Canceling a run에 기록.
> 러너가 소켓 `{"status":"canceled"}` 프레임과 폴링의 `canceled` 상태도 인지한다
> (모르는 상태=processing 규칙 때문에 이걸 안 하면 취소된 무비가 영원히 폴링했다).
> 크레딧 환급 문구는 정책 미확정이라 넣지 않았다(B-3와 같은 결정 대기).
> 실기기 검증 완료(2026-08-13, 실서버): 취소 → draft 복귀(컷 유지), 40초 후에도
> canceled 유지, 계속 만들기 → 런 지속 → ready 완주, 복귀한 draft 재생성 정상.
> 초소형 무비(컷 2개)는 확인 단계 응답보다 완성이 빠를 수 있다 — 그 경합은 409로
> 설계대로 흡수되는 것도 관찰함.

- **막힌 지점**: 편집 작업 취소 엔드포인트가 없다(`routes/edit-jobs.ts`에 POST/GET/WS만).
  계정 삭제 시 진행 중 작업을 취소하는 경로만 존재한다.
- **현재 사용자 경험**: `generating`은 푸터가 비고 편집이 얼며, 잘못 시작한 실행을 멈출 방법이
  화면 어디에도 없다([`movie-page.tsx:398`](src/pages/movie/ui/movie-page.tsx:398)).
  워커 타임아웃 10분이 사실상의 상한이다.
- **백엔드에 필요한 것**: `DELETE /edit-jobs/:id` 또는 동등한 취소 + 취소된 작업의 최종 상태
  정의(진행률 소켓 종료 방식 포함).
- **앱 쪽 후속**: 진행 화면에 취소를 두고, 취소된 무비가 `draft`로 돌아오는 규칙을
  `docs/features/movie.md`에 기록한다.
- **크레딧 모델과의 연결**: 크레딧 차감/환급이 정해지면(B-3) 취소 시 환급 여부가 취소 UI의
  문구를 결정한다. 순서상 B-3의 결정이 먼저다.

### B-3. 생성 비용의 사전 표시 — ⏳ 여전히 대기 (2026-08-13 확인)

> 이번 백엔드 반영(취소 API + errorCode)에 크레딧 API는 **포함되지 않았다**. 백엔드
> 저장소가 스스로 기록해 둔 상태(backlog A-2, 2026-08-13): 잔액 조회·1회 차감량·잔액
> 부족 거절 응답 셋 다 수량·가격 확정 전에는 계약을 못 정한다. 앱 쪽 후속은 그대로
> 대기하고, 아래 원문대로 흉내 내지 않는다.

- **현재 상태**: 월 3편 제한은 **집행되지 않는다**(2026-08-05 제거). 대신 크레딧 차감 모델이
  결정됐으나 크레딧 수량·가격·유효기간·무비 1편당 차감량이 **모두 미확정**
  (`credit-payment-model.md` §이번 결정에서 확정하지 않은 것, `backlog.md` A-2).
- **막힌 지점**: 차감량과 잔액 조회 API가 없으면 앱은 비용을 표시할 수 없다.
- **백엔드에 필요한 것**: 잔액 조회 + 1회 차감량 + 잔액 부족 시의 거절 응답 형태.
- **앱 쪽 후속**: `AI로 생성 시작` 옆 잔액/차감량 한 줄 read-out, 잔액 부족 거절을
  `refusal-notice.tsx`의 사유 목록에 추가. 원칙 8의 역방향 위반(`Hidden Cost`)은 그때 닫힌다.
- **지금 하지 말 것**: 확정되지 않은 숫자를 화면에 쓰는 것.

### B-4. 실패 메시지의 품질 — ✅ 2026-08-13 처리 완료 (백엔드 + 앱)

> 백엔드가 분류 코드를 구현했다: `errorCode`(`TIMEOUT | SOURCE_UNAVAILABLE |
> QUEUE_FAILED | INTERNAL`, append-only)가 `GET /edit-jobs/:id`와 WS 실패 프레임(`code`)에
> 실린다. 코드→앱 문구 매핑은 `refusal-notice.tsx`가 아니라
> `features/compose-movie/lib/edit-failure-copy.ts`에 두었다 — 실패 사유는 스토어에
> 저장돼(`Movie.error`) 무비 화면(pages)과 그리드 타일(widgets)이 같이 읽는데, 두 레이어는
> pages의 모듈을 import할 수 없다. 한 곳이라는 취지는 그대로다. 서버 원문은
> `Movie.errorDetail`로 내려 무비 화면 푸터에서만 보조 줄로 보인다(타일은 앱 문구만).
> 모르는 코드는 INTERNAL 취급(백엔드 지시). 실기기에서는 실패를 실제로 유발할 수 없어
> (건강한 백엔드, 워커 타임아웃 10분) 실패 분기를 잠깐 강제해 앱 문구 + 강등된 진단
> 원문 렌더링만 확인하고 되돌렸다(A-7 선례). 매핑·전송 경로는 유닛 테스트가 덮는다.

- **현재 상태**: 앱은 서버 메시지를 그대로 그린다
  ([`generate-footer.tsx:107`](src/pages/movie/ui/generate-footer.tsx:107),
  [`movie-failure-notice.tsx:36`](src/widgets/movie-shelf/ui/movie-failure-notice.tsx:36)).
  `docs/features/movie.md`도 "메시지 자체는 아직 사용자용 품질이 아니다"라고 인정한다.
- **백엔드에 필요한 것**: 실패 사유의 **분류 코드**. 문구만 다듬어도 앱은 여전히 분기할 수
  없으므로, 코드가 사람 문구보다 먼저다.
- **앱 쪽 후속**: 코드→앱 문구 매핑을 `refusal-notice.tsx` 한 곳에 두고(그게 그 모듈이 존재하는
  이유다), 서버 원문은 접어둔 진단으로 내린다. 원칙 16, `Leaky Vocabulary`.
- **부분적으로 지금 가능**: 앱이 자체적으로 만드는 실패(마지막 원본 삭제, 404 job)는 이미 앱
  문구다. 그 둘만이라도 서버 원문과 시각적으로 구분되게 하는 것은 A 항목으로 내릴 수 있다.

---

## C. 문서 정정 (코드 변경 없음, 먼저 처리)

앱 문서가 백엔드의 현행 사실과 어긋나 있어, 이 상태로는 다음 작업자가 잘못된 전제를 물려받는다.
`AGENTS.md`의 "문서와 구현이 어긋나면 예외를 퍼뜨리지 말고 갱신하거나 보고한다"에 해당한다.

### C-1. 월 3편 한도 서술 (`docs/features/movie.md`) — ✅ 2026-08-13 처리 완료

- **현재 서술**: `rejected` 거절 사유를 "소유권, `ready` 상태, **무료 플랜의 월 3편 한도**"로
  적고, 2026-08-10에 "그 달 네 번째 실행이 서버의 플랜 제한 문장을 그대로 보여줬다"고
  기기 검증 결과까지 기록해 두었다(§Before a run starts).
- **백엔드 현행**: 월 3편 제한은 2026-08-05에 제거됐고 기계적으로 복원하지 않는다
  (`plan-limits.md` §0-2, §3). 즉 검증 당시의 관찰은 사실이었지만 정책은 그 뒤 바뀌었다.
- **할 일**: 해당 서술을 현행(플랜 차등 미집행 + 크레딧 모델 전환 결정)으로 갱신한다.
  `403 FORBIDDEN`을 서버 문구 그대로 전달한다는 앱의 처리 방식 자체는 변하지 않는다.

### C-2. 자막 플래그 (`docs/features/movie.md` → Subtitles 행)

- **현재 서술**: "스펙에 `subtitles` 플래그(기본 false)가 생겼지만 앱이 아직 보내지 않는다"
  — 이건 정확하다. 정정이 아니라 **판단이 필요한 항목**으로 남겨둔다.
- **논점**: 백엔드가 이제 끌 수 있게 됐으므로, 2026-08-07에 스위치를 제거한 근거
  ("결정하는 게 없는 컨트롤")는 더 이상 성립하지 않는다. 다만 소프트 자막(mov_text)을
  `expo-video`가 렌더링하지 않는 문제는 그대로라, 켜도 앱에서 안 보인다.
- **할 일**: 이 문서에서 결정하지 않는다. A-3(BGM)과 같은 종류의 제품 결정이므로
  사용자에게 올린다.

---

## 권장 순서

1. **C-1** — 잘못된 전제부터 걷어낸다. 코드 변경 없음.
2. **A-3 / B-1의 갈림길 결정**(BGM을 없앨 것인가, API를 요구할 것인가) — 이 결정이
   A-2의 facts line 구성까지 정한다.
3. **A-2, A-1** — 거짓 상태 표시와 막다른 빈 상태. 사용자 체감이 가장 크다.
4. **A-4 ~ A-9** — 순서 무관, 각각 독립.
5. **B-2, B-3, B-4** — 백엔드 백로그에 올리고 대기. 앱에서 흉내 내지 않는다.

각 항목은 `npm run verify`를 통과해야 하고, 화면 변경은
`docs/workflows/android-device-verification.md`에 따라 실기기에서 확인한다.
UX 변경이 동작을 바꾸면 `docs/features/movie.md`를 같은 변경 안에서 갱신한다(가드레일 G20).
